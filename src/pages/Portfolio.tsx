import Menubar from "@/containers/Menubar/Header";

const Portfolio = () => {
  return (
    <main className="flex border-primary w-full h-full main-background">
      <Menubar />
    </main>
  );
};

export default Portfolio;
